<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
    <title>Login Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/styles/theme.css" rel="stylesheet">
</head>
<body class="d-flex justify-content-center align-items-center vh-100 bg-dark">
    <div class="card glass-panel p-4 shadow-lg login-card">
        <h3 class="text-center text-info mb-4">🔒 ALICE ADMIN</h3>
        <% if(error) { %> <div class="alert alert-danger"><%= error %></div> <% } %>
        <form action="/login" method="POST">
            <div class="mb-3">
                <label class="text-light">Password Akses</label>
                <input type="password" name="password" class="form-control bg-dark text-white" required>
            </div>
            <button class="btn btn-primary w-100">Masuk</button>
        </form>
    </div>
</body>
</html>
